<h1>
	Photos for <%= current_user.first_name %>
</h1>

<% @photos.each do |photo| %>
<% if photo.user_id == current_user.id%>
  
  <%= link_to (image_tag (photo.userphoto.url(:small))), photo %>
 
  
<%end%>
<%end%>
 <br />
 <br />
 <%= link_to 'Home', root_path %>
