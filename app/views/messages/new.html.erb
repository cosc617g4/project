<%= form_for @message, :url => messages_path, :method => :post do |f| %>

<%= f.collection_select :to, @friends, :email, :fullname, :prompt => "Select a Friend" %>
<%= f.text_area :body %>
<%= f.text_field :topic %>
<%= f.button :submit, class: 'btn' %>
<% end %>

